# Dév local : copier vers client/.env
VITE_API_URL=http://localhost:3000

# En production, c'est client/.env.production (déjà dans le repo) qui est utilisé :
# VITE_API_URL=https://gcave-api.onrender.com
